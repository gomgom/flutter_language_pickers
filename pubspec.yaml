name: language_pickers
description: It makes language select pickers for you in Flutter. You can use it freely in lots of ways.
version: 0.4.0
homepage: https://github.com/gomgom/flutter_language_pickers
repository: https://github.com/gomgom/flutter_language_pickers
issue_tracker: https://github.com/gomgom/flutter_language_pickers/issues
topics:
  - language
  - picker
  - dropdown
  - i18n
  - localization

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

#flutter:
#  assets:
#  - assets/
